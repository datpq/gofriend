<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:control="clr-namespace:goFriend.Views"
             xmlns:local="clr-namespace:goFriend"
             Title="{Static local:res.BasicInfos}"
             x:Class="goFriend.Views.AccountBasicInfosPage">
    <ContentPage.BackgroundColor>
        <OnPlatform x:TypeArguments="Color">
            <On Platform="Android, WinPhone">
                <StaticResource Key="ColorTabPageBackground" />
            </On>
            <On Platform="iOS">White</On>
        </OnPlatform>
    </ContentPage.BackgroundColor>
    <ContentPage.Content>
        <StackLayout Margin="10">
            <StackLayout.Padding>
                <OnPlatform x:TypeArguments="Thickness">
                    <On Platform="Android, WinPhone">0</On>
                    <On Platform="iOS">0,60,0,0</On>
                </OnPlatform>
            </StackLayout.Padding>
            <control:UcLabelEntry x:Name="LeLastName" LabelText="{Static local:res.LastName}" />
            <control:UcLabelEntry x:Name="LeFirstName" LabelText="{Static local:res.FirstName}" />
            <control:UcLabelEntry x:Name="LeFullName" LabelText="{Static local:res.FullName}" IsEnabled="False" />
            <control:UcLabelEntry x:Name="LeEmail" LabelText="{Static local:res.Email}" IsEnabled="False" />
            <Label Text="{Static local:res.Gender}" />
            <Picker x:Name="PkGender" Title="{Static local:res.SelectGender}" TitleColor="{StaticResource ColorPrimary}" />
            <Label x:Name="LblBirthDay" Text="{Static local:res.BirthDay}" />
            <DatePicker x:Name="DpBirthDay" />
            <Button HorizontalOptions="FillAndExpand" Margin="0,10"
                    CornerRadius="{StaticResource BtnCornerRadius}"
                    BackgroundColor="{StaticResource ColorPrimary}"
                    TextColor="{StaticResource ColorTitle}"
                    Text="{x:Static local:res.Save}"
                    Clicked="CmdSave_Click" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>